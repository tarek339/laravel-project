import { BreadcrumbItem } from "@/types";

const TrailerProfile = () => {
    const breadcrumbs: BreadcrumbItem[] = [
        {
            title: "Trailer Profile",
            href: "/trailer",
        },
    ];

    return <div>TrailerProfile</div>;
};

export default TrailerProfile;
